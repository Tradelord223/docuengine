[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "docuengine"
version = "0.1.0"
description = "Local-first autonomous documentary editing engine skeleton"
requires-python = ">=3.11"

[project.scripts]
docuengine = "docuengine.cli:main"

